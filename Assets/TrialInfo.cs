using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using System;
using Microsoft.Win32;

public class TrialInfo : MonoBehaviour {

	//const string userRoot = "HKEY_CURRENT_USER";
	//const string subkey = "RegistrySetValueExample";
	//const string keyName = userRoot + "\\" + subkey;

	//void Start () {
	//	int requestCode = (int)Registry.GetValue(keyName, PlayerPrefs.GetString("RequestCode"), -1);
	
	//	if (requestCode >= 0)
	//	{
	//		transform.GetComponent<Text>().text = "Trial Version. Remains: " + requestCode;
	//	}
	//}

	//void OnApplicationQuit()
	//{
	//	int requestCode = (int)Registry.GetValue(keyName, PlayerPrefs.GetString("RequestCode"), -1);

	//	if (requestCode > 0)
	//	{
	//		Registry.SetValue(keyName, PlayerPrefs.GetString("RequestCode"), requestCode - 1);
	//	}
	//}
}
